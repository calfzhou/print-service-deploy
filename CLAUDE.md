# Cloud-Printer Deploy (docker branch)

This repo maintains a Dockerfile and related configs reverse-engineered from the `tzishue/cloud-printer` Docker image.

## Key task

When the upstream Docker image is updated, sync this branch to match. See `.claude/skills/sync-from-image.md` for the full procedure.
